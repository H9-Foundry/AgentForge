---
"@h9-foundry/agentforge-cli": patch
---

Fix the published CLI package so global npm installs support direct `agentforge` execution again, and add release verification coverage for PATH-based global-prefix installs.
