---
"@h9-foundry/agentforge-cli": patch
---

Fix package-user AI-Gorilla dogfood gaps by improving repo-fit inference for complex app repositories, allowing bounded `.github/workflows/...` evidence paths, and resolving matching root `package.json` release targets during `release-readiness`.
